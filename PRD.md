以下为您定制的《AI对话平台网站产品需求文档（PRD）》。文档包含了技术栈推荐以及详细的功能模块拆解，采用标准的Markdown格式，方便您直接复制使用或导入到语雀、飞书等文档工具中。

***

# AI对话平台网站 产品需求文档 (PRD)

## 1. 文档概述
### 1.1 项目背景
本项目旨在构建一个部署于局域网内的AI对话平台，底层依托已部署在Ubuntu服务器上的本地大语言模型（Qwen系列），为局域网用户提供私有化、安全、高效的AI对话与API调用服务。

### 1.2 项目目标
- 实现基于本地大模型的流式在线对话功能。
- 提供标准化的API接口及开发者文档，支持局域网内其他系统调用。
- 实现完善的用户鉴权、对话隔离、Token统计及后台管理功能。

---

## 2. 技术栈推荐与架构设计
考虑到项目需在Windows环境开发，最终部署至Ubuntu，且需长期稳定运行并处理AI流式响应，推荐以下技术栈：

| 层级       | 技术选型                                                    | 推荐理由                                                     |
| ---------- | ----------------------------------------------------------- | ------------------------------------------------------------ |
| **前端**   | **Vue 3 + Vite + TypeScript + Tailwind CSS + Element Plus** | Vue3生态成熟，Vite编译速度快；Tailwind适合快速构建现代化UI；Element Plus提供丰富的基础组件，适合中后台及对话界面开发。 |
| **后端**   | **Python + FastAPI + SQLAlchemy**                           | FastAPI原生支持异步和SSE（Server-Sent Events），完美契合AI流式对话需求；Python与AI生态无缝衔接，方便后续扩展RAG或Agent功能。 |
| **数据库** | **PostgreSQL**                                              | 支持JSONB数据类型（非常适合存储多轮对话历史和复杂配置），高并发下性能稳定，适合长期部署。 |
| **部署**   | **Docker + Docker Compose + Nginx**                         | 保证Windows开发与Ubuntu部署的环境一致性；Nginx负责反向代理、静态资源托管及HTTPS/局域网域名解析。 |

---

## 3. 用户角色与权限
| 角色         | 权限说明                                                     |
| ------------ | ------------------------------------------------------------ |
| **游客**     | 仅能访问首页(`/`)、登录(`/login`)、注册(`/register`)、找回密码(`/recovery`)页面。 |
| **普通用户** | 登录后拥有完整的在线对话、API调用、Token查看、API Key管理、个人资料修改权限。 |
| **管理员**   | 拥有普通用户所有权限，额外拥有后台管理(`/admin`)权限，可管理系统数据。 |

---

## 4. 核心功能需求 (MVP)

### 4.1 首页模块 (`/`)
- **布局**：页面居中或全屏分为左右两个大卡片（框框）。
- **左侧卡片**：在线对话入口 (`/chat`)，包含标题、简述及“进入对话”按钮。
- **右侧卡片**：API服务入口 (`/api`)，包含标题、简述及“进入API”按钮。
- **交互**：无需检测登录状态，点击对应卡片直接跳转。

### 4.2 认证模块
#### 4.2.1 注册 (`/register`)
- **字段**：用户名、电话号码、密码、确认密码。
- **规则**：
  - 电话号码需进行正则校验，仅支持中国大陆 `+86` 手机号（11位数字，1开头）。
  - 电话号码全局唯一，注册前需校验是否已存在。
  - 密码需进行强度校验（如至少8位，包含字母和数字），两次密码需一致。
  - **无需发送短信验证码**。

#### 4.2.2 登录 (`/login`)
- **字段**：电话号码、密码。
- **交互**：
  - 提供跳转至 `/register` 和 `/recovery` 的按钮/链接。
  - 登录成功后，根据用户角色跳转至默认页面（普通用户跳 `/chat`，管理员跳 `/chat` 但保留后台入口）。

#### 4.2.3 找回密码 (`/recovery`)
- **内容**：静态展示页面。显示提示文字（如：“请联系系统管理员重置密码”），并预留图片展示位（图片由管理员后续配置或硬编码）。

### 4.3 在线对话模块 (`/chat`)
#### 4.3.1 页面布局与路由拦截
- **路由拦截**：访问 `/chat` 时检测登录状态，未登录重定向至 `/login`。
- **顶部导航**：右上角提供跳转至 `/api` 的按钮。
- **左下角悬浮**：显示当前用户头像和用户名（详见4.7）。

#### 4.3.2 对话主界面
- **左侧边栏**：历史对话记录列表。点击“新建对话”生成新路由。
- **中右侧主窗口**：
  - **输入区**：文本输入框，支持多行。
  - **功能按钮**：
    - `深度思考`：开关按钮（开启后后端调用模型的思考参数或增强Prompt）。
    - `联网搜索`：开关按钮（开启后后端触发RAG检索或联网API）。
    - `+ 文件上传`：支持上传文档/图片，后端解析后作为Context传入模型。
  - **消息流**：支持Markdown渲染、代码高亮，**必须支持SSE流式打字机效果输出**。

#### 4.3.3 路由生成规则 (`/chat/[id]`)
- **规则**：`[id]` 格式为 `{电话号码明文}-{随机字符串}`。
- **示例**：`/chat/13800138000-a1b2c3d4e5f6`。
- **实现**：后端使用 `nanoid` 或 `uuid` 生成随机字符串，拼接手机号作为对话的唯一标识。

### 4.4 API服务模块 (`/api`)
#### 4.4.1 页面布局与路由拦截
- **路由拦截**：同 `/chat`，需登录。
- **顶部导航**：右上角提供跳转至 `/chat` 的按钮。
- **左侧菜单**：包含三个子菜单。

#### 4.4.2 子功能页面
1. **首次调用文档 (`/api` 或 `/api/index`)**：
   - 默认展示页。使用 Markdown 渲染。
   - 内容包含：接口基础URL、鉴权方式（Header传入API Key）。
   - 提供 `cURL`、`Python (requests)`、`Node.js (fetch/axios)` 的完整请求代码示例。
2. **Token用量 (`/api/token`)**：
   - 数据看板：展示当前用户的总消耗Token、今日消耗Token、剩余可用Token（如有配额限制）。
   - 图表：近7天/30天的Token消耗折线图/柱状图。
3. **API Key管理 (`/api/key`)**：
   - **查看**：展示当前已生成的 Key（部分隐藏，如 `sk-138****8000-a1b2...`）。
   - **生成**：点击生成新 Key。
   - **Key格式规则**：`sk-{电话号码}-{随机英文数字混合}`（如 `sk-13800138000-x7y8z9`）。
   - **操作**：支持复制、删除/禁用 Key。

### 4.5 用户中心模块 (`/user/[phone]`)
- **触发方式**：点击页面左下角的用户头像/名字，**在新标签页（新窗口）** 打开 `/user/{当前用户手机号}`。
- **页面内容**：
  - **基本信息**：展示用户名、手机号、注册时间。支持修改用户名。
  - **安全设置**：修改密码功能（需输入原密码、新密码、确认新密码）。
  - *注：本期暂不实现注销账号功能。*

### 4.6 后台管理模块 (`/admin`)
- **权限控制**：
  - 接口层与路由层双重校验，普通用户访问直接返回 `403 Forbidden`。
- **页面布局**：
  - 保持与 `/chat` 和 `/api` 相同的主体框架（左侧菜单、左下角用户信息）。
  - **右上角新增**：名为 `root` 的按钮，点击跳转至后台核心管理面板（具体账密及后台详细功能由后续补充，本期MVP仅做权限隔离和入口占位）。

### 4.7 全局组件：左下角用户信息卡片
- **展示范围**：除 `/`、`/admin`、`/login`、`/register` 之外的所有已登录页面。
- **样式**：固定在页面左下角，显示用户头像（默认首字母头像）和用户名。
- **交互**：点击该卡片，通过 `window.open()` 在新窗口打开 `/user/{phone}`。

---

## 5. 非功能性需求

### 5.1 性能要求
- **流式响应**：AI对话首字响应时间（TTFT）应 < 1秒（取决于本地模型推理速度），流式输出延迟 < 50ms。
- **并发支持**：后端需支持至少 50 个并发长连接（SSE）。

### 5.2 安全要求
- **鉴权**：采用 JWT (JSON Web Token) 进行状态保持，Token 需设置合理的过期时间（如7天），并支持 Refresh Token 机制。
- **API安全**：API Key 需在数据库中进行哈希或加密存储（或至少加盐哈希），前端展示时进行脱敏。
- **输入校验**：所有前端输入（尤其是手机号、密码、上传文件）必须在后端进行二次严格校验，防止 SQL 注入和 XSS 攻击。文件上传需限制类型（如仅允许 pdf, txt, png, jpg）和大小（如 < 10MB）。

### 5.3 兼容性
- 前端需适配主流现代浏览器（Chrome, Edge, Firefox, Safari），最低支持 1920x1080 分辨率，建议做响应式适配以支持 1366x768 笔记本屏幕。

---

## 6. 部署与运维方案

### 6.1 环境准备 (Ubuntu)
1. 安装 Docker 与 Docker Compose。
2. 确保本地大模型服务（如 Ollama, vLLM 或 llama.cpp）已正常运行，并暴露本地 API 端口（如 `127.0.0.1:11434`）。

### 6.2 容器化部署
编写 `docker-compose.yml`，包含以下服务：
- `frontend`: Nginx 托管 Vue 打包后的静态文件。
- `backend`: FastAPI 服务，暴露内部端口（如 `8000`）。
- `postgres`: PostgreSQL 数据库，挂载数据卷持久化。

### 6.3 Nginx 配置
配置 Nginx 作为统一入口（监听 80/443 端口）：
- `/` 及静态资源请求 -> 代理至 `frontend` 容器。
- `/api/v1/*` 等后端接口请求 -> 代理至 `backend` 容器。
- **关键配置**：针对 SSE 流式接口，需在 Nginx 中关闭缓冲：
  ```nginx
  proxy_buffering off;
  proxy_cache off;
  proxy_read_timeout 300s; # 防止长连接超时
  ```

### 6.4 数据备份
- 配置定时任务（Cron），每日凌晨对 PostgreSQL 数据库进行 `pg_dump` 物理备份，并保留最近 7 天的备份文件。

---

## 7. 后续迭代规划 (Out of Scope for MVP)
1. **多模型切换**：支持在界面上切换不同的本地模型（如 Qwen-0.8B, Qwen-7B 等）。
2. **知识库管理 (RAG)**：允许用户上传企业文档，构建专属知识库。
3. **管理员后台完善**：实现用户封禁、全局Token配额设置、系统日志查看等。
4. **消息推送**：对话完成或API调用异常时，通过手机邮件或局域网消息通知用户。